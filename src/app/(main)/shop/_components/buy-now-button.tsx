import { Button } from "@mui/material";
import React from "react";

const BuyNowButton = () => {
  return (
    <Button
      fullWidth
      variant="contained"
      color="primary"
    >
      Buy Now
    </Button>
  );
};

export default BuyNowButton;
