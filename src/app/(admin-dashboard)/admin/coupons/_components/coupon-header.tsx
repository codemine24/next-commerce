import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography";

export const CouponHeader = () => {
    return (
        <Box>
            <Typography variant="h3" mb={2}>Coupons</Typography>
        </Box>
    )
}